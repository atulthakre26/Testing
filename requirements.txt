# Core testing libraries
selenium==4.20.0
pytest==8.2.1
pytest-html==4.1.1
pytest-xdist==3.6.1

# Environment variable support
python-dotenv==1.0.1

# Logging and utilities (optional but helpful)
loguru==0.7.2

# Browser driver management (optional alternative to manual driver setup)
webdriver-manager==4.0.1

# Linting & formatting (optional but recommended)
flake8==7.0.0
black==24.4.2
